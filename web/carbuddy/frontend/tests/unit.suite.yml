suite_namespace: frontend\tests\unit
actor: UnitTester
modules:
    enabled:
        - Yii2:
            part: [orm, email, fixtures]
        - Asserts
        - Db
    config:
        Db:
            dsn: 'mysql:host=localhost;dbname=carbuddy'
            user: 'root'
            password: ''
            cleanup: true

